<h1>Burgers</h1>

<h2>Burgers that are not yet eaten.</h2>

<ul>
  {{#each burgers}}
    {{#unless burger_eaten}}
      {{> burgers/burger-block burger_eaten=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that have been devoured!</h2>

<ul>
  {{#each burgers}}
    {{#if burger_eaten}}
      {{> burgers/burger-block burger_eaten=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="burger_name">
  </div>

  <div class="form-group">
    <label for="burger_eaten">Burger Eaten?</label><br>
    <input type="radio" name="burger_eaten" value="1" checked> Devoured<br>
    <input type="radio" name="burger_eaten" value="0"> Taste it again
  </div>

  <button type="submit">Add Burger</button>
</form>